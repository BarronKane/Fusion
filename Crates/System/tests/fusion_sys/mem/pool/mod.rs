//! Integration-test canvas for `fusion_sys::mem::pool`.
//!
//! These tests focus on the pool contract itself: compatible member ownership, deterministic
//! fixed metadata, exact lease tracking, and allocator-facing extent semantics.

mod all;
